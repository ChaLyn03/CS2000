/**
 * Emulating OR operation using NAND and NOT gates.
 */

 CHIP Or {
    IN a, b;
    OUT out;

    PARTS:
        Not(a=in,out=nota)
        Not(b=in,out=notb)
        Nand(nota=in,notb=in,out=out)
}
