/**
 * Not gate:
 * out = not in
 */

 CHIP Not {
    IN in;
    OUT out;

    PARTS:
    // Tie both inputs of the NAND gate together to get the NOT behavior
    Nand(a=in, b=in, out=out);
}
